apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: communicator-websocket-gateway
  namespace: communicator-prod
  annotations:
    konghq.com/protocols: https
    konghq.com/https-redirect-status-code: '308'
    konghq.com/plugins: communicator-prod-cors
spec:
  tls:
    - hosts:
        - prod.websocket-gateway.communicator.kamilwronka.com
      secretName: communicator-prod-tls
  ingressClassName: kong
  rules:
    - host: prod.websocket-gateway.communicator.kamilwronka.com
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: communicator-websocket-gateway
                port:
                  number: 4000
