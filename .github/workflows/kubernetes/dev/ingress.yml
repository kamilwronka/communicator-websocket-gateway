apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: communicator-websocket-gateway
  namespace: communicator-dev
  annotations:
    konghq.com/protocols: https
    konghq.com/https-redirect-status-code: '308'
    konghq.com/plugins: communicator-dev-auth0-jwt,communicator-dev-cors
spec:
  tls:
    - hosts:
        - dev.websocket-gateway.communicator.kamilwronka.com
      secretName: communicator-dev-tls
  ingressClassName: kong
  rules:
    - host: dev.websocket-gateway.communicator.kamilwronka.com
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: communicator-websocket-gateway
                port:
                  number: 4000
