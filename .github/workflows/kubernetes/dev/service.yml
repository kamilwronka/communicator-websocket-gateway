apiVersion: v1
kind: Service
metadata:
  name: communicator-websocket-gateway
  labels:
    app: communicator-websocket-gateway
  namespace: communicator-dev
spec:
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app: communicator-websocket-gateway
